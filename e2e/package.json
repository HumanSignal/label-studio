{
  "name": "label-studio-e2e",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "cd .. && rm -rf _e2e_project && label-studio start --init _e2e_project -b",
    "client": "sleep 2 && codeceptjs run --steps",
    "client:headless": "sleep 2 && HEADLESS=true codeceptjs run --steps",
    "test": "npm-run-all -p -r server client",
    "test:headless": "npm-run-all -p -r server client:headless"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "codeceptjs": "^2.6.10",
    "npm-run-all": "^4.1.5",
    "puppeteer": "^5.2.1"
  }
}
