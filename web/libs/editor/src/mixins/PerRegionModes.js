/**
 * Mirror of PerRegionModes.ts for environments that resolve .js before .ts.
 * Keep in sync with PerRegionModes.ts.
 */
export const PER_REGION_MODES = {
  TAG: "tag",
  REGION_LIST: "region-list",
};
